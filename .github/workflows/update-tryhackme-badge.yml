name: Update TryHackMe Badge

permissions:
  contents: write

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-badge:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        persist-credentials: true
        fetch-depth: 0

    - name: Configure Git Identity
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"

    - name: Fetch TryHackMe Badge
      uses: DhanushNehru/tryhackme-badge-action-workflow@v1.0
      continue-on-error: true
      with:
        image_path: './assets/tryhackme-badge.png'
        username: 'dcollins50'
        user_id: '69024e3a066cc67bf24fd67c'
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Commit and push badge if changed
      run: |
        git add ./assets/tryhackme-badge.png || true
        if git diff --cached --quiet; then
          echo "No changes to commit"
          exit 0
        fi
        git commit -m "Updated THM profile badge"
        git push origin "HEAD:${{ github.ref_name }}"
